export enum TaskStatus {
  Completed = 'completed',
  InProgress = 'in progress',
  Backlog = 'backlog',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SortBy {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}
